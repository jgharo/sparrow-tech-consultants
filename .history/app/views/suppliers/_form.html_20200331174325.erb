<%= form_with(model: supplier, local: true) do |form| %>
  <% if supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supplier.errors.count, "error") %> prohibited this Supplier from being saved:</h2>

      <ul>
        <% supplier.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :supplier_name, "Name" %>
    <%= form.text_field :supplier_name %>
  </div>

  <div class="field">
    <%= form.label :supplier_email, "Name" %>
    <%= form.text_field :supplier_email %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.collection_select :product_id %>
  </div>

  <div class="field">
    <%= form.label :supplierpaymentinfo_id %>
    <%= form.collection_select :supplierpaymentinfo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

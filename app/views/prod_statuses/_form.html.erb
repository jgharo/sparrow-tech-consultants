<%= form_with(model: prod_status, local: true) do |form| %>
  <% if prod_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prod_status.errors.count, "error") %> prohibited this prod_status from being saved:</h2>

      <ul>
        <% prod_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prod_status %>
    <%= form.text_field :prod_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

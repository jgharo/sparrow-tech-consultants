<%= form_with(model: servcategory, local: true) do |form| %>
  <% if servcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(servcategory.errors.count, "error") %> prohibited this servcategory from being saved:</h2>

      <ul>
        <% servcategory.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

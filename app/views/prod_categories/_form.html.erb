<%= form_with(model: prod_category, local: true) do |form| %>
  <% if prod_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prod_category.errors.count, "error") %> prohibited this prod_category from being saved:</h2>

      <ul>
        <% prod_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prod_category %>
    <%= form.text_field :prod_category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
